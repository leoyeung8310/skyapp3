import { FiveStarsPipe } from './five-stars.pipe';

describe('FiveStarsPipe', () => {
  it('create an instance', () => {
    const pipe = new FiveStarsPipe();
    expect(pipe).toBeTruthy();
  });
});
